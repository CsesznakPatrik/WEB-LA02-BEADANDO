<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    	<nav>
    <ul class="actions">
        <li><a href="/">Főoldal</a></li>

        <% if (currentUser) { %> <li><a href="/messages">Üzenetek</a></li>
            
            <% if (currentUser.isAdmin == 1) { %>
                <li><a href="/admin-route">Admin Kezelés</a></li>
            <% } %>
            
            <li><a href="/logout">Kijelentkezés</a></li>

        <% } else { %>
            <li><a href="/login">Bejelentkezés</a></li>
            <li><a href="/register">Regisztráció</a></li>

        <% } %>
    </ul>
</nav>
   <% if (currentUser && (currentUser.isAdmin == 1 || currentUser.isAdmin == 0)) { 
%>
    <h2>Beérkezett Üzenetek</h2>
    
    <% if (messages.length > 0) { %>
        <ul>
            <% messages.forEach(msg => { %>
                <li>
                    <strong>Feladó:</strong> <%= msg.name %> (<%= msg.email %>)
                    <br>
                    <strong>Tárgy:</strong> <%= msg.subject %>
                    <br>
                    <strong>Üzenet:</strong> <%= msg.message %>
                    <hr>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>Még nem érkezett üzenet az adatbázisba.</p>
    <% } %>
<% } %>
</body>
</html>